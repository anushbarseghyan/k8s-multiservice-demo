kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: k8s-demo
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # frontend NodePort
        hostPort: 8080
        listenAddress: "127.0.0.1"
        protocol: TCP
